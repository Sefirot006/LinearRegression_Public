#include "linearRegression.h"
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <cstdlib>
#include <unistd.h>

using namespace std;

int
main(int argc,char** argv){
    // Trainning
	//Trainning_Set train("train.csv");
	//LinearReg reg(train.get_dim(),0.00000001);
	//train.normalize();
	//reg.normal_equation(train);

	return 1;
}
